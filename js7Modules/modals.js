import { slidersSelectors, sliderData } from "./sliders.js";
import { tabsSelectors } from "./tabs.js";

//IMPORTING DIFF OBJECTS FROM OTHER MODULES

export function initModals() {
  console.log("modals init");
}

const modalsSelector = {
  name: "modals",
};

console.log(slidersSelectors);
console.log(sliderData);
console.log(tabsSelectors);
