const slidersSelectors = {
  name: "sliders",
};

const sliderData = {};

export default () => {
  console.log("init sliders");
};

//EXPORTING MAIN MAIN FUNCTION USING default
//IMPLEMENTING DEFAULT ARROW FUNCION WITHOUT EVEN NAMING IT

export { slidersSelectors, sliderData };

//EXPORTING SEVERAL OBJECTS AT ONE TIME
